# 작업 기록 (2026-03-02)

## 범위
- 세션 로그아웃 고도화
- 예약 승인 플로우 도입
- 교사 일정 생성 UX 개선(드래그 후 모달)
- 교사 불가 일정 UX 개선(캘린더 드래그 후 모달)
- 페이지 분할 및 사이드 메뉴 구성
- 모바일 대응 보강

## 상세 변경
- 백엔드
  - `backend/src/auth.js`: 토큰 `jti` 발급/검증, 폐기 토큰 체크
  - `backend/src/index.js`:
    - 예약 생성 `PENDING`
    - 교사 승인 API 추가
    - 슬롯 점유 조건(`PENDING`,`BOOKED`) 반영
    - 시간표 메타데이터(제목/메모) 저장/조회 반영
  - 마이그레이션
    - `003_auth_token_revocations.sql`
    - `004_booking_pending_approval.sql`
    - `005_availability_lesson_metadata.sql`
- 프론트
  - `frontend/index.html`: 로그인/역할 라우팅 유지
  - `frontend/teacher.html`, `frontend/student.html`:
    - 드래그 생성 모달
    - 불가 일정 생성 모드(`수업 오픈`/`불가 시간·휴무`) 추가
    - 날짜 선택 + 종일 차단 + 시간 차단 모달 추가
    - 예외 저장 시 선생님 주간 캘린더 즉시 재렌더링 반영
    - 승인대기 상태 표시
    - 401 시 세션 초기화 후 로그인 페이지 이동
    - 사이드 메뉴 + 분할 섹션 표시 로직
  - 분할 페이지 추가
    - `teacher-calendar.html`, `teacher-manage.html`, `teacher-bookings.html`
    - `student-calendar.html`, `student-bookings.html`

## 검증 로그
- `docker compose run --rm backend npm test` -> PASS
- `bash "/Volumes/Extreme SSD/workspace/scripts/smoke-test-api.sh"` -> PASS
- `docker compose exec -T backend npm run -s migrate` -> PASS
- `docker compose up --build -d backend frontend` -> PASS

## 메모
- 교사/학생 페이지는 공통 코드 기반이므로 기능 수정 시 양쪽 동기화 확인 필수

## 추가 작업 (블럭 예약/예외/초대/공휴일)
- 예약 단위 변경
  - 고정 수업길이 기반 분할 대신, 교사가 만든 슬롯 블럭 자체를 예약 단위로 사용
  - `09:15~10:15`, `10:30~11:00` 같은 비정시각/가변 길이 블럭도 학생 화면에서 그대로 노출/예약됨
- 1회성 수업 오픈 구조 개선
  - 단일 생성(`repeat_mode=single`)은 예외 대량 생성이 아닌 `one_time_availabilities`에 1건 저장
  - 신규 마이그레이션: `006_one_time_availabilities.sql`
  - API 추가:
    - `GET /api/v1/teachers/me/one-time-availability`
    - `POST /api/v1/teachers/me/one-time-availability`
    - `DELETE /api/v1/teachers/me/one-time-availability/:id`
- 예외 처리 단순화
  - 예외 등록을 블럭 단위(시작/종료 필수)로 강제
  - 예외 겹침/충돌 검증 유지, 캘린더에서 해당 블럭만 차단
- 학생 초대 기능
  - 교사 화면에 `학생 초대 링크 복사` 버튼 추가
  - 링크 형식: `/student-calendar.html?teacher=<teacherId>`
  - 학생이 링크로 접속하면 해당 교사가 기본 선택됨
- 한국 공휴일 표시
  - KR 공휴일 데이터를 연도 캐시로 로드해 학생/교사 캘린더 헤더와 셀에 표시
- 페이지 동기화
  - `teacher.html` 변경사항을 분할 페이지(`teacher-*`, `student-*`)에 동기화

## 추가 검증
- `docker compose run --rm backend npm test` -> PASS (7/7)
- `bash "/Volumes/Extreme SSD/workspace/scripts/smoke-test-api.sh"` -> PASS
- API 재현 검증:
  - `09:15~10:15`(60분), `10:30~11:00`(30분) 블럭 조회/예약 정상
  - `09:15~10:15` 예외 등록 시 해당 블럭만 비노출 처리 확인

## 추가 작업 (게스트/코멘트/설정파일)
- 설정파일 기반 운영값 확장
  - `backend/src/config.js` + `backend/config/app.config.json` 추가/확장
  - 게스트 PIN 락아웃, 공개 링크 보존일, 게스트 데이터 보존일, 게스트 취소사유 필수, 완료 코멘트 필수 정책을 설정으로 제어
- 예약 완료 코멘트 분리
  - 교사용 내부 메모: `teacher_private_comment`
  - 학생 전달 코멘트: `student_comment`
  - 완료 API에서 두 코멘트를 분리 저장
- 게스트 예약 보안/운영 보강
  - PIN 실패 횟수 누적 후 락아웃
  - 교사 PIN 초기화 시 실패횟수/락 상태 초기화
  - 게스트 취소(전화번호+PIN, 토큰링크)에서 취소 사유 필수
- DB 마이그레이션
  - `007_booking_completion_and_teacher_comment.sql`
  - `008_guest_student_booking_access.sql`
  - `009_guest_pin_lockout_and_comment_split.sql`
- 프론트 동작 정리
  - 학생 목록: 학생용 코멘트만 표시
  - 교사 목록: 학생용 코멘트 + 내부 메모 분리 표시
  - 완료/코멘트 수정 시 두 코멘트를 각각 입력받아 전송
  - 게스트 취소 시 취소 사유 필수 입력
  - `teacher.html` 기준으로 분할 페이지 전체 동기화 유지

## 추가 검증 (최신)
- `docker-compose build backend` -> PASS
- `docker-compose run --rm backend npm test` -> PASS (15/15)
- `docker-compose up --build -d backend frontend` -> PASS
- `docker-compose exec -T backend npm run -s migrate` -> PASS (`001~009`)
- `bash "/Volumes/Extreme SSD/workspace/scripts/smoke-test-api.sh"` -> PASS

## 추가 작업 (2026-03-06)
- 학생 취소 정책 변경
  - 학생(회원/비회원) 취소 허용 시점을 `수업 전날 N시` 정책으로 통일
  - 기본값 `21시`, 교사별 설정값(`student_cancel_day_before_hour`)으로 관리
  - 교사 취소는 기존처럼 제한 없이 가능
- 교사 공지사항 추가
  - `teacher_profiles.student_notice` 추가
  - 학생 화면에 선택한 선생님 공지사항 표시
  - 교사 설정 폼에서 공지/취소시각 직접 저장 가능
- 시간표 중복 생성 방지
  - `weekly_availabilities`, `one_time_availabilities`, `availability_exceptions` 중복 제거 + 유니크 인덱스 적용
  - 마이그레이션: `010_teacher_cancel_policy_notice_and_dedup.sql`
- 교사 대리 예약 기능 추가
  - API: `POST /api/v1/teachers/me/bookings`
  - 회원 이메일 기반/비회원(이름+휴대폰+PIN) 기반 예약 지원
  - 상태는 즉시 `BOOKED`로 생성
- 예약 링크 분리
  - 캘린더 링크: `/student-calendar.html?teacher=<id>`
  - 레슨 링크: `/student-calendar.html?teacher=<id>&start_at=<ISO>`
  - 교사 캘린더에 “이번 주 오픈 슬롯(링크/대리예약)” 목록 추가
- 예약 승인/취소 탭 버그 수정
  - 교사의 `거절/취소` 버튼이 동작하지 않던 문제 수정 (`cancelBooking` 권한 분기 보완)
- 완료수업 UI 정리
  - 완료 이력 카드에서 “완료시각(버튼 누른 시각)” 표기를 제거해 혼동 축소

## 추가 검증 (2026-03-06)
- `docker-compose exec -T backend npm test` -> PASS (19/19)
- `docker-compose up --build -d backend frontend` -> PASS
- `node --check backend/src/index.js` -> PASS
- `node --check /tmp/teacher_script.js` (`teacher.html` 스크립트 추출본) -> PASS
