# Handover - 2026-03-06

## 오늘 반영한 핵심
- 학생 취소 정책을 `수업 전날 N시` 기준으로 전환
- 교사 공지사항(`student_notice`) 및 취소시각(`student_cancel_day_before_hour`) 교사별 설정 가능
- 시간표/예외 중복 생성 방지 마이그레이션 추가 (`010`)
- 교사 대리예약 API + UI(오픈 슬롯 목록에서 대리예약)
- 예약 링크 분리(캘린더 링크/레슨 링크)
- 교사 예약 탭의 거절/취소 버튼 미동작 수정
- 완료수업 이력에서 완료시각 표기 제거

## 스키마/마이그레이션
- 신규: `db/migrations/010_teacher_cancel_policy_notice_and_dedup.sql`
  - `teacher_profiles.student_cancel_day_before_hour` (0~23, default 21)
  - `teacher_profiles.student_notice`
  - 중복 데이터 정리 + 유니크 인덱스 추가

## 백엔드 주요 변경 파일
- `backend/src/index.js`
  - 교사 프로필 API 확장
  - 학생/비회원 취소 컷오프 계산 변경
  - 교사 대리예약 API 추가
  - 중복 에러 처리 코드 추가
- `backend/config/app.config.json`
  - `booking.studentCancelDayBeforeHourLocal` 추가
- `backend/test/bookings.integration.test.js`
  - 신규/변경 정책 테스트 반영

## 프론트 주요 변경 파일
- `frontend/teacher.html` (그리고 동일 내용이 분할 페이지로 동기화됨)
  - 학생 공지 패널 추가
  - 교사 설정 폼(취소시각/공지/타임존) 추가
  - 오픈 슬롯 목록(레슨 링크 복사/대리예약) 추가
  - 레슨 링크(`start_at`) 진입 시 해당 슬롯 강조
  - 교사 취소 버튼 동작 분기 수정

## 검증
- `docker-compose exec -T backend npm test` -> PASS (19/19)
- `docker-compose up --build -d backend frontend` -> PASS

## 주의
- 분할 페이지는 현재 `teacher.html`을 복사 동기화하는 방식이므로, 후속 수정 시 동일 동기화 필요
- 신규 스키마 사용 전 `npm run migrate` 또는 테스트/배포 파이프라인 마이그레이션 보장 필요
