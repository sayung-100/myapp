# 인수인계 노트 (2026-02-19)

## 오늘 완료된 작업
- 커밋/배포 상태
  - 로컬 커밋: `334da52`
  - 메시지: `feat: 시간표/예약 API와 캘린더 UI 구현`
  - 원격 반영: `origin/main` 푸시 완료
- 주요 구현 범위
  - 백엔드: 시간표/예외/슬롯/예약 API 추가
    - `/api/v1/teachers/me/availability` CRUD
    - `/api/v1/teachers/me/exceptions` 조회/등록/삭제
    - `/api/v1/teachers`, `/api/v1/teachers/:teacherId/slots`
    - `/api/v1/bookings` 생성, `/api/v1/bookings/me`, `/api/v1/teachers/me/bookings`, `/api/v1/bookings/:id/cancel`
  - 프론트: 역할 기반(teacher/student) 화면 분리 + 캘린더 기반 예약 UI
  - 문서: `README.md` 엔드포인트/현재 단계 업데이트
  - 프록시: `frontend/nginx.conf` `/api` 프록시 수정

## 오늘 검증 결과 (스모크 테스트)
- 통과 시나리오
  - 헬스체크: backend/front proxy
  - teacher/student 로그인
  - teacher 시간표 보장(중복 시 409 허용)
  - student 슬롯 조회
  - student 예약 생성
  - student 예약 취소
  - teacher 예약 목록 반영 확인
- 재현 스크립트
  - `/Volumes/Extreme SSD/workspace/scripts/smoke-test-api.sh`

## 실행 순서 (Gemini CLI 동료용)
1. Docker 런타임 시작
   - `colima start`
2. 스택 실행
   - `docker-compose up --build -d`
3. DB 반영
   - `docker-compose run --rm backend npm run migrate`
   - `docker-compose run --rm backend npm run seed`
4. 스모크 테스트
   - `bash "/Volumes/Extreme SSD/workspace/scripts/smoke-test-api.sh"`

## 현재 주의사항
- Codex 샌드박스 환경에서는 Docker socket/localhost 접근이 간헐적으로 막힐 수 있음.
  - 실제 로컬 터미널(Gemini CLI 포함)에서는 동일 명령이 정상 동작하는지 먼저 확인 권장.
- 스모크 테스트는 데이터 생성/취소를 수행하므로 개발 DB 기준으로만 실행 권장.

## 다음 작업 제안
- 프론트 UI를 모듈화(현재 `frontend/index.html` 단일 파일이 큼)
- 예약/시간표 API에 대한 자동화 테스트(통합 테스트) 추가
- 예약 정책 엣지 케이스 보강
  - 타임존 경계
  - 취소 컷오프 경계 시간
  - 동시 예약 충돌
